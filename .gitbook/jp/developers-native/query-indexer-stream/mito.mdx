---
title: Mito
updatedAt: "2025-11-28"
---

StreamManagerV2を使用してMito vault module関連データをindexerからストリーミングするコードスニペットの例です。

## StreamManagerV2を使用したgRPCストリーム

### Vaultのtransferをストリーミング

```ts
import { getNetworkEndpoints, Network } from '@injectivelabs/networks'
import { 
  StreamManagerV2,
  IndexerGrpcMitoStreamV2 
} from '@injectivelabs/sdk-ts/client/indexer'

const endpoints = getNetworkEndpoints(Network.Testnet)
const stream = new IndexerGrpcMitoStreamV2(endpoints.indexer)

const streamManager = new StreamManagerV2({
  id: 'mito-transfers',
  streamFactory: () => stream.streamTransfers({ 
    vault: 'inj...', // オプション
    account: 'inj...', // オプション
    callback: (response) => {
      streamManager.emit('data', response)
    }
  }),
  onData: (transfers) => {
    console.log(transfers)
  },
  retryConfig: { enabled: true }
})

streamManager.on('connect', () => console.log('Stream connected'))
streamManager.start()
```

### Vaultの更新をストリーミング

```ts
import { getNetworkEndpoints, Network } from '@injectivelabs/networks'
import { 
  StreamManagerV2,
  IndexerGrpcMitoStreamV2 
} from '@injectivelabs/sdk-ts/client/indexer'

const endpoints = getNetworkEndpoints(Network.Testnet)
const stream = new IndexerGrpcMitoStreamV2(endpoints.indexer)

const streamManager = new StreamManagerV2({
  id: 'mito-vault',
  streamFactory: () => stream.streamVault({ 
    vault: 'inj...', // オプション
    callback: (response) => {
      streamManager.emit('data', response)
    }
  }),
  onData: (vaultUpdate) => {
    console.log(vaultUpdate)
  },
  retryConfig: { enabled: true }
})

streamManager.on('connect', () => console.log('Stream connected'))
streamManager.start()
```

### Vault holderのsubscriptionをストリーミング

```ts
import { getNetworkEndpoints, Network } from '@injectivelabs/networks'
import { 
  StreamManagerV2,
  IndexerGrpcMitoStreamV2 
} from '@injectivelabs/sdk-ts/client/indexer'

const endpoints = getNetworkEndpoints(Network.Testnet)
const stream = new IndexerGrpcMitoStreamV2(endpoints.indexer)

const streamManager = new StreamManagerV2({
  id: 'mito-vault-holder-subscriptions',
  streamFactory: () => stream.streamVaultHolderSubscriptions({ 
    holderAddress: 'inj...',
    vaultAddress: 'inj...', // オプション
    stakingContractAddress: 'inj...', // オプション
    callback: (response) => {
      streamManager.emit('data', response)
    }
  }),
  onData: (subscriptions) => {
    console.log(subscriptions)
  },
  retryConfig: { enabled: true }
})

streamManager.on('connect', () => console.log('Stream connected'))
streamManager.start()
```

### Accountごとのstaking rewardをストリーミング

```ts
import { getNetworkEndpoints, Network } from '@injectivelabs/networks'
import { 
  StreamManagerV2,
  IndexerGrpcMitoStreamV2 
} from '@injectivelabs/sdk-ts/client/indexer'

const endpoints = getNetworkEndpoints(Network.Testnet)
const stream = new IndexerGrpcMitoStreamV2(endpoints.indexer)

const streamManager = new StreamManagerV2({
  id: 'mito-staking-rewards',
  streamFactory: () => stream.streamStakingRewardsByAccount({ 
    staker: 'inj...',
    stakingContractAddress: 'inj...',
    callback: (response) => {
      streamManager.emit('data', response)
    }
  }),
  onData: (rewards) => {
    console.log(rewards)
  },
  retryConfig: { enabled: true }
})

streamManager.on('connect', () => console.log('Stream connected'))
streamManager.start()
```

### Historical stakingをストリーミング

```ts
import { getNetworkEndpoints, Network } from '@injectivelabs/networks'
import { 
  StreamManagerV2,
  IndexerGrpcMitoStreamV2 
} from '@injectivelabs/sdk-ts/client/indexer'

const endpoints = getNetworkEndpoints(Network.Testnet)
const stream = new IndexerGrpcMitoStreamV2(endpoints.indexer)

const streamManager = new StreamManagerV2({
  id: 'mito-historical-staking',
  streamFactory: () => stream.streamHistoricalStaking({ 
    staker: 'inj...',
    stakingContractAddress: 'inj...',
    callback: (response) => {
      streamManager.emit('data', response)
    }
  }),
  onData: (historicalStaking) => {
    console.log(historicalStaking)
  },
  retryConfig: { enabled: true }
})

streamManager.on('connect', () => console.log('Stream connected'))
streamManager.start()
```
