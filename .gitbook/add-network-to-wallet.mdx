---
title: Add Network to Your Wallet
description: >-
  Add Injective networks to your browser wallet in one click. Interactive
  buttons for Injective EVM (MetaMask and other EVM wallets) and Injective
  native (Keplr), covering mainnet and testnet.
icon: wallet
updatedAt: "2026-07-13"
---

Add Injective to your browser wallet with a single click, or use the manual
configuration details below. Injective exposes two interoperable interfaces:

- **Injective EVM** — an Ethereum-compatible JSON-RPC layer. Works with EVM
  wallets such as MetaMask, Rabby, or any injected `window.ethereum` wallet.
- **Injective (CosmWasm / native)** — the native Cosmos SDK chain. Works with
  Cosmos wallets such as Keplr and Leap.

<Info>
Injective EVM and Injective native map to the **same** network — they use
different chain IDs but share the same underlying state. See
[Network Information](/developers/network-information) and
[EVM Network Information](/developers-evm/network-information) for the full reference.
</Info>

export const AddNetworkButton = ({ label, sublabel, onClick }) => {
  const [status, setStatus] = useState("idle");
  const handle = async () => {
    try {
      setStatus("pending");
      await onClick();
      setStatus("done");
      setTimeout(() => setStatus("idle"), 2500);
    } catch (e) {
      console.error(e);
      setStatus("error");
      setTimeout(() => setStatus("idle"), 3500);
    }
  };
  const text =
    status === "pending" ? "Confirm in your wallet…" :
    status === "done" ? "Added ✓" :
    status === "error" ? "Failed — try again" : label;
  return (
    <button
      type="button"
      onClick={handle}
      disabled={status === "pending"}
      className="not-prose"
      style={{
        display: "flex", alignItems: "center", gap: "12px", width: "100%",
        maxWidth: "420px", padding: "12px 16px", marginBottom: "10px",
        borderRadius: "12px", border: "1px solid rgba(77,61,255,0.35)",
        background: status === "error" ? "#ffefef" : "rgba(77,61,255,0.06)",
        color: "#182E4B", fontWeight: 600, fontSize: "15px",
        cursor: status === "pending" ? "wait" : "pointer", textAlign: "left",
      }}
    >
      <span
        style={{
          display: "inline-flex", alignItems: "center", justifyContent: "center",
          width: "34px", height: "34px", borderRadius: "9px",
          background: "#4D3DFF", color: "#fff", fontSize: "18px", flexShrink: 0,
        }}
      >
        {status === "done" ? "✓" : "＋"}
      </span>
      <span style={{ display: "flex", flexDirection: "column" }}>
        <span>{text}</span>
        {sublabel && (
          <span style={{ fontWeight: 400, fontSize: "12.5px", opacity: 0.7 }}>
            {sublabel}
          </span>
        )}
      </span>
    </button>
  );
};

export const addEvmNetwork = (config) => async () => {
  if (!window.ethereum) {
    alert("Please install MetaMask or another EVM wallet.");
    throw new Error("No EVM wallet found");
  }
  try {
    await window.ethereum.request({
      method: "wallet_switchEthereumChain",
      params: [{ chainId: config.chainId }],
    });
  } catch (err) {
    if (err && err.code === 4902) {
      await window.ethereum.request({
        method: "wallet_addEthereumChain",
        params: [config],
      });
    } else {
      throw err;
    }
  }
};

export const enableCosmos = (chainId) => async () => {
  if (!window.keplr) {
    alert("Please install the Keplr wallet.");
    throw new Error("No Keplr wallet found");
  }
  await window.keplr.enable(chainId);
};

export const INJ_EVM_MAINNET = {
  chainId: "0x6f0", // 1776
  chainName: "Injective",
  rpcUrls: ["https://sentry.evm-rpc.injective.network/"],
  nativeCurrency: { name: "Injective", symbol: "INJ", decimals: 18 },
  blockExplorerUrls: ["https://blockscout.injective.network/"],
};

export const INJ_EVM_TESTNET = {
  chainId: "0x59f", // 1439
  chainName: "Injective Testnet",
  rpcUrls: ["https://k8s.testnet.json-rpc.injective.network/"],
  nativeCurrency: { name: "Injective", symbol: "INJ", decimals: 18 },
  blockExplorerUrls: ["https://testnet.blockscout.injective.network/"],
};

## Injective EVM

Injective EVM is compatible with EVM wallets (MetaMask, Rabby, and more). Click a
button below — your wallet will prompt you to add or switch to the network.

<AddNetworkButton
  label="Add Injective EVM Mainnet"
  sublabel="MetaMask · Rabby · any EVM wallet — Chain ID 1776"
  onClick={addEvmNetwork(INJ_EVM_MAINNET)}
/>

<AddNetworkButton
  label="Add Injective EVM Testnet"
  sublabel="MetaMask · Rabby · any EVM wallet — Chain ID 1439"
  onClick={addEvmNetwork(INJ_EVM_TESTNET)}
/>

<Callout icon="info" color="#07C1FF" iconType="regular">
The button uses the standard `wallet_addEthereumChain` / `wallet_switchEthereumChain`
requests. If the network is already added, your wallet shows a "switch network"
prompt instead. If nothing happens, add it manually with the values below.
</Callout>

### Manual configuration

<Tabs>

<Tab title="Injective EVM Mainnet" icon="server">

| Property | Value |
| --- | --- |
| Network Name | Injective |
| Chain ID | `1776` |
| RPC URL | `https://sentry.evm-rpc.injective.network/` |
| WS Endpoint | `wss://sentry.evm-ws.injective.network` |
| Currency Symbol | `INJ` |
| Decimals | `18` |
| Block Explorer | `https://blockscout.injective.network/` |

</Tab>

<Tab title="Injective EVM Testnet" icon="flask-vial">

| Property | Value |
| --- | --- |
| Network Name | Injective Testnet |
| Chain ID | `1439` |
| RPC URL | `https://k8s.testnet.json-rpc.injective.network/` |
| WS Endpoint | `wss://k8s.testnet.ws.injective.network/` |
| Currency Symbol | `INJ` |
| Decimals | `18` |
| Block Explorer | `https://testnet.blockscout.injective.network/` |
| Faucet | [`testnet.faucet.injective.network`](https://testnet.faucet.injective.network/) |

</Tab>

</Tabs>

## Injective (CosmWasm / Native)

The native Injective chain works with Cosmos wallets such as **Keplr** and **Leap**.
Injective is built into both wallets, so it is available out of the box. Click a
button below to unlock the chain in Keplr.

<AddNetworkButton
  label="Connect Injective Mainnet in Keplr"
  sublabel="Keplr · Leap — Chain ID injective-1"
  onClick={enableCosmos("injective-1")}
/>

<AddNetworkButton
  label="Connect Injective Testnet in Keplr"
  sublabel="Keplr · Leap — Chain ID injective-888"
  onClick={enableCosmos("injective-888")}
/>

<Callout icon="info" color="#07C1FF" iconType="regular">
Keplr and Leap ship with Injective built in, so the button simply unlocks the
chain in your wallet. For a full wallet integration guide, see [Wallets](/defi/wallet/index).
</Callout>

### Manual configuration

<Tabs>

<Tab title="Injective Mainnet" icon="server">

| Property | Value |
| --- | --- |
| Chain ID | `injective-1` |
| RPC / Node | `https://sentry.tm.injective.network:443` |
| Currency Symbol | `INJ` |
| Decimals | `18` |
| Explorer | [`injscan.com`](https://injscan.com/) |

<Info>
Injective natively uses the chain ID `injective-1`. Its EVM interface uses the
numeric chain ID `1776` — both map to the same mainnet.
</Info>

</Tab>

<Tab title="Injective Testnet" icon="flask-vial">

| Property | Value |
| --- | --- |
| Chain ID | `injective-888` |
| RPC / Node | `https://testnet.sentry.tm.injective.network:443` |
| Currency Symbol | `INJ` |
| Decimals | `18` |
| Explorer | [`testnet.explorer.injective.network`](https://testnet.explorer.injective.network/) |
| Faucet | [`testnet.faucet.injective.network`](https://testnet.faucet.injective.network/) |

<Info>
Injective natively uses the chain ID `injective-888` on testnet. Its EVM interface
uses the numeric chain ID `1439` — both map to the same testnet.
</Info>

</Tab>

</Tabs>
