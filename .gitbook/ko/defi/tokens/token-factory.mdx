---
title: Token Factory
updatedAt: "2026-09-02"
---

TokenFactory 토큰은 체인의 bank 모듈에 기본적으로 통합되어 있습니다. 이름은 `factory/{creatorAddress}/{subdenom}` 형식을 취합니다. 토큰이 생성자 주소로 네임스페이스되기 때문에 이름 충돌을 해결할 필요가 없어 무허가 토큰 발행이 가능합니다.

이 통합은 스마트 컨트랙트를 직접 쿼리해야 하는 CW20 표준과 달리 모든 자산의 총 공급량을 추적하고 쿼리하는 것을 지원합니다. 이러한 이유로 TokenFactory 표준 사용이 권장됩니다. 예를 들어 Helix나 Mito와 같은 제품은 bank 토큰만 사용하는 Injective exchange 모듈에 구축되어 있습니다. TokenFactory 토큰은 injectived CLI를 통해서뿐만 아니라 스마트 컨트랙트를 통해서도 생성할 수 있습니다. Wormhole을 통해 Injective로 브릿지된 토큰도 TokenFactory 토큰입니다.

## CW20 어댑터

`CW20AdapterContract`는 CW20 자산을 Injective에서 Token Factory denom으로 네이티브하게 표현할 수 있게 합니다. CW20 자산은 어댑터 컨트랙트에 보관되고 Injective 주소를 위한 factory denom으로 발행됩니다. 다시 CW20으로 상환할 때는 bank 모듈에서 소각되고 어댑터 컨트랙트에서 잠금 해제되어 소유자 주소로 돌아갑니다.

### factory denom을 CW20으로 상환하기

```ts
import {
  MsgExecuteContractCompat,
  ExecArgCW20AdapterRedeemAndTransfer,
} from '@injectivelabs/sdk-ts/core/modules'

const CW20_ADAPTER_CONTRACT = 'inj...'
const contractCw20Address = 'inj...'
const injectiveAddress = 'inj...'

const message = MsgExecuteContractCompat.fromJSON({
  sender: injectiveAddress,
  contractAddress: CW20_ADAPTER_CONTRACT,
  funds: {
    denom: `factory/${CW20_ADAPTER_CONTRACT}/${contractCw20Address}`,
    amount: actualAmount.toFixed(),
  },
  execArgs: ExecArgCW20AdapterRedeemAndTransfer.fromJSON({
    recipient: injectiveAddress,
  }),
})

// Then pack the message in a transaction, sign it and broadcast to the chain
```

### CW20을 factory denom으로 변환하기

```ts
import {
  ExecArgCW20Send,
  MsgExecuteContractCompat,
} from '@injectivelabs/sdk-ts/core/modules'

const CW20_ADAPTER_CONTRACT = 'inj...'
const contractCw20Address = 'inj...'
const injectiveAddress = 'inj...'
const amount = '1000000' // 1 USDT represented as on the chain as it has 6 decimals

const message = MsgExecuteContractCompat.fromJSON({
  contractAddress: contractCw20Address,
  sender: injectiveAddress,
  execArgs: ExecArgCW20Send.fromJSON({
    amount,
    contractAddress: CW20_ADAPTER_CONTRACT,
  }),
})

// Then pack the message in a transaction, sign it and broadcast to the chain
```

## 더 읽어보기

- [토큰 런칭 가이드](../../developers-defi/token-launch/) - Injective에서 토큰을 만드는 단계별 가이드
- [Token Factory 모듈 참조](../../developers-native/injective/tokenfactory/) - 상세 모듈 사양
